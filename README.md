<!-- # React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Oxc](https://oxc.rs)
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/)

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for information on how to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project. -->
You are a Senior Frontend Engineer and Senior UI/UX Designer.

I have attached the complete source code of my React + Vite project named "HostelConnect". Before making ANY changes, first analyze the entire project structure carefully.

Your workflow must be:

STEP 1 - ANALYSIS
- Explore every folder and component.
- Understand the current architecture.
- Analyze all CSS files, layouts, reusable components, routing, pages, and assets.
- Identify duplicate styles, inconsistent spacing, poor typography, outdated UI patterns, unnecessary CSS, and responsiveness issues.
- Understand the design language already being used.

Do NOT start modifying anything until you understand the whole project.

------------------------------------------------

STEP 2 - UI/UX REFINEMENT

This is NOT a redesign.

Do NOT rebuild the project from scratch.

Do NOT replace pages with completely different layouts.

Instead, preserve the existing structure while professionally refining and polishing every screen.

Think of it as turning a college project into a production-quality web application.

Maintain all existing functionality, routing, and component hierarchy.

------------------------------------------------

Design Goals

The application is a Hostel Complaint & Resolution System used by college students and hostel wardens.

The UI should feel:

- modern
- clean
- trustworthy
- premium
- professional
- slightly elegant
- student friendly
- suitable for a college administration portal

Avoid looking like:
- AI-generated templates
- generic dashboards
- random admin panels
- colorful startup landing pages
- over-designed Dribbble concepts

The design should feel realistic and actually deployable in a college.

------------------------------------------------

Color Palette

Keep the existing primary color theme.

Do NOT completely change branding.

Refine the colors instead.

Use better shades, gradients where appropriate, improved contrast, proper hierarchy, and modern neutral backgrounds.

The interface should look premium without becoming flashy.

------------------------------------------------

Typography

Improve:

- font hierarchy
- spacing
- readability
- heading sizes
- paragraph spacing
- button typography
- sidebar typography
- navbar typography

Everything should look balanced and polished.

------------------------------------------------

Spacing

Fix inconsistent:

- padding
- margins
- gaps
- card spacing
- section spacing
- alignment

Everything should follow a consistent spacing system.

------------------------------------------------

Cards

Redesign existing cards without changing their purpose.

Improve:

- border radius
- shadows
- depth
- elevation
- hover animations
- transitions
- spacing
- icon placement

Cards should feel modern and slightly elevated.

------------------------------------------------

Tables

Improve complaint tables.

Add:

- better row spacing
- hover highlights
- modern headers
- cleaner status badges
- improved readability
- responsive behavior

------------------------------------------------

Buttons

Refine all buttons.

Improve:

- hover effects
- active state
- focus state
- border radius
- transitions
- shadows where appropriate

Buttons should feel clickable and premium.

------------------------------------------------

Forms

Improve all forms.

Focus on:

- input spacing
- labels
- validation appearance
- focus rings
- dropdown styling
- textarea styling
- image upload UI if present

------------------------------------------------

Sidebar

Refine the sidebar.

Improve:

- active state
- hover animations
- icons
- spacing
- visual hierarchy

Do NOT redesign the navigation structure.

------------------------------------------------

Navbar / Header

Improve:

- spacing
- alignment
- typography
- profile section
- notification icons
- subtle shadows
- sticky behavior if appropriate

------------------------------------------------

Animations

Add tasteful micro-interactions.

Examples:

- hover transitions
- smooth card elevation
- button animations
- page transitions
- fade-ins
- subtle scaling
- smooth dropdown animations

Animations should be smooth and professional.

Avoid excessive motion.

------------------------------------------------

3D Feel

Introduce subtle depth using:

- layered shadows
- glass effects only where appropriate
- slight gradients
- elevated cards
- soft borders

Avoid heavy glassmorphism.

Avoid neon.

Avoid futuristic cyberpunk styles.

------------------------------------------------

Responsive Design

Ensure every page works properly on:

- desktop
- laptop
- tablet
- mobile

Fix overflow, spacing, and layout issues without changing the existing page structure.

------------------------------------------------

Code Quality

While refining:

- remove duplicate CSS
- simplify styles where possible
- improve maintainability
- avoid unnecessary inline styles
- reuse common classes
- keep components clean

------------------------------------------------

Performance

Avoid unnecessary re-renders.

Do not introduce heavy UI libraries unless absolutely necessary.

Prefer CSS over animation libraries.

------------------------------------------------

Accessibility

Improve:

- focus visibility
- color contrast
- button accessibility
- semantic HTML where applicable

------------------------------------------------

Very Important

DO NOT:

- change business logic
- change API structure
- rename routes
- break existing functionality
- replace the entire UI
- convert everything into a new design system

Only enhance what already exists.

------------------------------------------------

Expected Result

The final application should look like a polished production-ready college portal that could realistically be used by thousands of students.

It should look significantly more premium than before while still feeling like the same HostelConnect application.

Whenever possible, explain WHY you made a particular UI improvement before implementing it.

Work incrementally, page by page, rather than making random changes throughout the project.